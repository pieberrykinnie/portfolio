/** @type {import('next').NextConfig} */
const nextConfig = {
        output: 'export',
        basePath: '/portfolio',
        images: {
                path: "/",
        },
};

export default nextConfig;
